import { Link } from "react-router-dom";

const Start = () => {
  return (
    <div className="text-box">
      <Link to="/insight" class="btn btn-white btn-animated">
        Старт
      </Link>
    </div>
  );
};

export default Start;
